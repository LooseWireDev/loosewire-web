---
// src/components/farscry/Nav.astro
import Wordmark from './Wordmark.astro';
---
<nav class="fc-nav">
  <a href="/farscry" class="fc-nav-home">
    <Wordmark logoSize={24} fontSize="15px" />
  </a>
  <div class="fc-nav-links">
    <a href="/farscry/blog">Blog</a>
    <a href="#features">Features</a>
    <a href="https://github.com/LooseWireDev/farscry" class="fc-nav-cta">GitHub</a>
  </div>
  <button class="fc-nav-burger" aria-label="Toggle menu" aria-expanded="false">
    <svg class="fc-burger-icon" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <line class="fc-burger-top" x1="3" y1="6" x2="21" y2="6" />
      <line class="fc-burger-mid" x1="3" y1="12" x2="21" y2="12" />
      <line class="fc-burger-bot" x1="3" y1="18" x2="21" y2="18" />
    </svg>
  </button>
  <div class="fc-nav-mobile">
    <a href="/farscry/blog">Blog</a>
    <a href="#features">Features</a>
    <a href="https://github.com/LooseWireDev/farscry" class="fc-nav-cta">GitHub</a>
  </div>
</nav>

<script>
  const burger = document.querySelector('.fc-nav-burger');
  const mobileMenu = document.querySelector('.fc-nav-mobile');
  if (burger && mobileMenu) {
    burger.addEventListener('click', () => {
      const open = mobileMenu.classList.toggle('is-open');
      burger.classList.toggle('is-open', open);
      burger.setAttribute('aria-expanded', String(open));
    });
    mobileMenu.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        mobileMenu.classList.remove('is-open');
        burger.classList.remove('is-open');
        burger.setAttribute('aria-expanded', 'false');
      });
    });
  }
</script>

<style>
  .fc-nav {
    max-width: 1100px;
    margin: 0 auto;
    padding: 20px 32px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
  }
  .fc-nav-home {
    text-decoration: none;
  }
  .fc-nav-links {
    display: flex;
    gap: 24px;
    align-items: center;
  }
  .fc-nav-links a {
    font-size: 14px;
    color: var(--fc-text-secondary);
    text-decoration: none;
  }
  .fc-nav-cta {
    font-size: 13px !important;
    font-weight: 600;
    color: var(--fc-text-inverse) !important;
    background: var(--fc-violet);
    padding: 8px 18px;
    border-radius: 8px;
  }

  .fc-nav-burger {
    display: none;
    background: none;
    border: none;
    color: var(--fc-text-primary);
    cursor: pointer;
    padding: 4px;
  }

  .fc-nav-mobile {
    display: none;
  }

  @media (max-width: 640px) {
    .fc-nav-links {
      display: none;
    }
    .fc-nav-burger {
      display: block;
    }
    .fc-nav-mobile {
      display: none;
      flex-direction: column;
      width: 100%;
      gap: 4px;
      padding-top: 16px;
    }
    .fc-nav-mobile.is-open {
      display: flex;
    }
    .fc-nav-mobile a {
      font-size: 15px;
      color: var(--fc-text-secondary);
      text-decoration: none;
      padding: 10px 0;
      border-bottom: 1px solid var(--fc-border-subtle);
    }
    .fc-nav-mobile .fc-nav-cta {
      display: inline-block;
      text-align: center;
      margin-top: 8px;
      border-bottom: none;
    }

    .fc-burger-top,
    .fc-burger-mid,
    .fc-burger-bot {
      transition: transform 0.2s ease, opacity 0.2s ease;
      transform-origin: center;
    }
    .fc-nav-burger.is-open .fc-burger-top {
      transform: translateY(6px) rotate(45deg);
    }
    .fc-nav-burger.is-open .fc-burger-mid {
      opacity: 0;
    }
    .fc-nav-burger.is-open .fc-burger-bot {
      transform: translateY(-6px) rotate(-45deg);
    }
  }
</style>
